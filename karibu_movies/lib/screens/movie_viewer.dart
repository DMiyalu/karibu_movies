import 'package:flutter/material.dart';

class MovieViewer extends StatelessWidget {
  const MovieViewer({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Movie Viewer'),
    );
  }
}
